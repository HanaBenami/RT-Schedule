git readme VX
- add TypeScript

TypeScript VX
- git issue with files rename
- test everyuthing! 
- redux
-- git rid of "as unknown as AnyAction"
-- all actions / types are "UNKNOWN" - why ?? 

Backend tests ? > add coverage to readme

AWS:
deployment
add build status, app status and link to readme
dashboard
ssl certificate
